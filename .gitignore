# OS / editor
.DS_Store
/.claude

# Raw data (3.8GB, hosted on GEO / private)
raw_data/

# Intermediate processed data (regenerable from raw via scripts)
processed_data/

# Large R binary objects (regenerable, not git-friendly)
# Keep .tsv/.csv/.png/.pdf in results/, exclude .rds
*.rds

# R workspace / history
.Rhistory
.RData
.Ruserdata
.Rproj.user/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Logs / tmp
*.log
*.tmp
/tmp/

# Sensitive
*.env
.env
reports/full_pipeline_report.html
reports/pipeline_report_v2.html

# 大体积全表 (320K 行 / 64K 行) — 已 gzip 压缩存储
results/DEG_5cell_dose/DEG_full_5cell_4contrast.tsv
results/DEG_5cell_dose/Mo_dose_DEG_full.tsv
